{
  "name": "web-components-study",
  "version": "1.0.0",
  "description": "A project for studying and experimenting with web components.",
  "scripts": {
    "prebuild": "rimraf dist",
    "build": "rollup -c",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Franky Cheung",
  "license": "ISC",
  "devDependencies": {
    "rimraf": "^3.0.2",
    "rollup": "^2.32.1"
  }
}
